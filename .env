# Modo demostración: true = datos simulados, sin solicitudes reales.
VITE_DEMO_MODE=false

# URL base del backend FastAPI en producción (sin barra final).
VITE_API_BASE_URL=https://molpredict-egfr-api--ibanezmariela78.replit.app

# Timeout de las solicitudes en milisegundos.
VITE_API_TIMEOUT_MS=30000
